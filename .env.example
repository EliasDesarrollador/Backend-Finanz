# Backend Finanz - Archivo de ejemplo de variables de entorno
# Copia este archivo como ".env" en esta misma carpeta y completa los valores.
# Nota: En Railway se inyectan automáticamente las variables MYSQLHOST, MYSQLPORT,
# MYSQLUSER, MYSQLPASSWORD y MYSQLDATABASE. Para local, usa las DB_* de abajo.

# =====================
# Configuración de MySQL (Local)
# =====================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=expense_tracker

# =====================
# Configuración del servidor
# =====================
# Puerto de la API
PORT=3000
# URL pública del backend (ajustar al dominio del backend)
SERVER_BASE_URL=http://localhost:3000
# URL del frontend a la que redirigir tras OAuth si no se pasa ?redirect
OAUTH_REDIRECT_DEFAULT=http://localhost:5173/auth

# =====================
# Credenciales OAuth (opcional)
# =====================
# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# =====================
# Notas para Railway
# =====================
# Railway provee estas variables automáticamente cuando adjuntas MySQL:
#   MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE
# Si existen, el backend las usará en lugar de DB_* automáticamente.
